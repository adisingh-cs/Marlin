I need to create a user registration system for a web application. The system should accept a username, email address, and password from new users. It must validate that the email is properly formatted, ensure the password meets minimum security requirements of at least 8 characters with one uppercase letter and one number, check that the username is unique in the database, hash the password using bcrypt before storing it, and return the newly created user profile without the password field.

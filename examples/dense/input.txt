Build an authentication middleware function that validates JWT tokens from the request headers, checks the token against the database for expiration and revocation status, and returns an unauthorized response if the token is invalid or expired. Include comprehensive error handling for missing tokens, malformed tokens, and database connection failures. The middleware should support both access tokens and refresh tokens, implement token rotation on refresh, and log all authentication attempts to a monitoring endpoint.
